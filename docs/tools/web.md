# Web UI (`horto-os-ui-web`)

Leptos CSR + rangular colocated `.html` / `.scss` panels. Built with Trunk; embedded by Tauri and servable in a browser later.

```bash
make desktop-web
make desktop-web-serve   # http://localhost:4187
```

Screens: Overview, Connection, Services. Theme: light / dark / system.
