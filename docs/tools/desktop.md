# Desktop (`horto-os-ui-desktop`)

Tauri 2 homeowner app. Native File / Edit / View / Help menu; webview loads `horto-os-ui-web`. No privileged remote install.

```bash
make desktop
make build-desktop
```

Menu actions refresh status, switch screens, cycle theme, zoom, About.
